{% extends 'base.html.twig' %}

{% block body %}
    <h1>diner creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('diner_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
