{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modifier :<br>{{ personne.nom }} {{ personne.prenom }}</h1>

    <div class="center">
        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Enregistrer" />
        <input type="button" value="Annuler" back_href="{{ path('personne_index') }}" />
        {{ form_end(edit_form) }}

    </div>

{% endblock %}
